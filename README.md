# steamos-java
Make Java usable in SteamOS by aliasing "java" to the path of a Java executable. Intended use with the official Minecraft Launcher (available via Flathub) but should work with anything else that uses Java.
